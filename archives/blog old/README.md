# Archived blog pages (Astro)

These static `.astro` blog pages were moved here so the live site serves **only** posts from `src/content/blog` (MDX / Content Collections) via the dynamic route `src/pages/blog/[slug].astro`.

- **Do not delete** these files; they are kept for reference or future use.
- Live blog posts are defined in `src/content/blog/*.mdx` and rendered by `[slug].astro`.
