---
export const prerender = false;

import AdminLayout from '../../layouts/AdminLayout.astro';
import { getSession } from '../../lib/auth';
import {
	LOCKER_SIDE_OPTIONS,
	LOCKER_BUILD_TOP_OPTIONS,
	LOCKER_BUILD_BOTTOM_OPTIONS,
	lockerBuildImagePath,
} from '../../lib/crm-locker-build-form';

const session = await getSession(Astro.cookies);
if (!session) return Astro.redirect('/admin/login');
---

<AdminLayout title="Locker build form" activeNav="locker-build-form">
	<div class="lbf-card card">
		<p class="lbf-lead">
			Choose <strong>one side profile</strong> (4 types), <strong>one top</strong> (4 types), and <strong>one bottom</strong> (3 types). Draft only — not saved to the database yet.
		</p>

		<form class="lbf-form" id="lbf-form">
			<fieldset class="lbf-fieldset">
				<legend class="lbf-legend">Sides — pick 1 of 4 (elevation / profile)</legend>
				<div class="lbf-grid lbf-grid--sides">
					{LOCKER_SIDE_OPTIONS.map((opt, i) => {
						const src = lockerBuildImagePath(opt.imageFile);
						return (
							<label class="lbf-choice">
								<input type="radio" name="locker_side" value={opt.id} checked={i === 0} />
								<span class="lbf-choice-body">
									<span class="lbf-choice-media">
										{src ? (
											<>
												<img class="lbf-img" src={src} alt="" width="200" height="140" loading="lazy" data-lbf-img />
												<span class="lbf-img-missing" hidden data-lbf-missing>
													Missing: <code>{opt.imageFile}</code>
												</span>
											</>
										) : (
											<span class="lbf-img-missing" data-lbf-missing>No image configured</span>
										)}
									</span>
									<span class="lbf-choice-text">
										<span class="lbf-choice-title">{opt.label}</span>
										{opt.description && <span class="lbf-choice-desc">{opt.description}</span>}
									</span>
								</span>
							</label>
						);
					})}
				</div>
			</fieldset>

			<fieldset class="lbf-fieldset">
				<legend class="lbf-legend">Top of locker — pick 1 of 4</legend>
				<div class="lbf-grid lbf-grid--top">
					{LOCKER_BUILD_TOP_OPTIONS.map((opt, i) => {
						const src = lockerBuildImagePath(opt.imageFile);
						return (
							<label class="lbf-choice">
								<input type="radio" name="locker_top" value={opt.id} checked={i === 0} />
								<span class="lbf-choice-body">
									<span class="lbf-choice-media">
										{src ? (
											<>
												<img class="lbf-img" src={src} alt="" width="200" height="140" loading="lazy" data-lbf-img />
												<span class="lbf-img-missing" hidden data-lbf-missing>
													Missing: <code>{opt.imageFile}</code>
												</span>
											</>
										) : (
											<span class="lbf-img-missing" data-lbf-missing>No image configured</span>
										)}
									</span>
									<span class="lbf-choice-text">
										<span class="lbf-choice-title">{opt.label}</span>
										{opt.description && <span class="lbf-choice-desc">{opt.description}</span>}
									</span>
								</span>
							</label>
						);
					})}
				</div>
			</fieldset>

			<fieldset class="lbf-fieldset">
				<legend class="lbf-legend">Bottom of locker — pick 1 of 3</legend>
				<div class="lbf-grid lbf-grid--bottom">
					{LOCKER_BUILD_BOTTOM_OPTIONS.map((opt, i) => {
						const src = lockerBuildImagePath(opt.imageFile);
						return (
							<label class="lbf-choice">
								<input type="radio" name="locker_bottom" value={opt.id} checked={i === 0} />
								<span class="lbf-choice-body">
									<span class="lbf-choice-media">
										{src ? (
											<>
												<img class="lbf-img" src={src} alt="" width="200" height="140" loading="lazy" data-lbf-img />
												<span class="lbf-img-missing" hidden data-lbf-missing>
													Missing: <code>{opt.imageFile}</code>
												</span>
											</>
										) : (
											<span class="lbf-img-missing" data-lbf-missing>No image configured</span>
										)}
									</span>
									<span class="lbf-choice-text">
										<span class="lbf-choice-title">{opt.label}</span>
										{opt.description && <span class="lbf-choice-desc">{opt.description}</span>}
									</span>
								</span>
							</label>
						);
					})}
				</div>
			</fieldset>

			<div class="lbf-summary card" id="lbf-summary">
				<span class="lbf-summary-label">Current selection</span>
				<p class="lbf-summary-line" id="lbf-summary-line">—</p>
			</div>

			<p class="lbf-footnote">
				Next step (when you’re ready): save side + top + bottom IDs on an order in Supabase, or export a PDF from this page.
			</p>
		</form>
	</div>

	<script is:inline>
		(function () {
			var form = document.getElementById('lbf-form');
			var out = document.getElementById('lbf-summary-line');
			if (!form || !out) return;

			function labelFor(name, value) {
				var input = form.querySelector('input[name="' + name + '"][value="' + value + '"]');
				if (!input) return value;
				var lab = input.closest('label');
				if (!lab) return value;
				var t = lab.querySelector('.lbf-choice-title');
				return t ? t.textContent.trim() : value;
			}

			function refresh() {
				var side = form.querySelector('input[name="locker_side"]:checked');
				var top = form.querySelector('input[name="locker_top"]:checked');
				var bot = form.querySelector('input[name="locker_bottom"]:checked');
				var s = side ? labelFor('locker_side', side.value) : '—';
				var a = top ? labelFor('locker_top', top.value) : '—';
				var b = bot ? labelFor('locker_bottom', bot.value) : '—';
				out.textContent = 'Side: ' + s + ' · Top: ' + a + ' · Bottom: ' + b;
			}

			form.addEventListener('change', refresh);
			refresh();

			document.querySelectorAll('[data-lbf-img]').forEach(function (img) {
				img.addEventListener('error', function () {
					img.style.display = 'none';
					var ph = img.parentElement && img.parentElement.querySelector('[data-lbf-missing]');
					if (ph) ph.hidden = false;
				});
			});
		})();
	</script>
</AdminLayout>

<style>
	.lbf-lead {
		font-size: 15px;
		line-height: 1.6;
		color: #4b5563;
		margin: 0 0 24px 0;
		max-width: 820px;
	}

	.lbf-lead code {
		font-size: 12px;
		background: #f3f4f6;
		padding: 2px 6px;
	}

	.lbf-fieldset {
		border: none;
		margin: 0 0 32px 0;
		padding: 0;
	}

	.lbf-legend {
		font-family: 'Oswald', sans-serif;
		font-size: 14px;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.1em;
		color: #374151;
		margin: 0 0 14px 0;
		padding: 0;
	}

	.lbf-grid {
		display: grid;
		gap: 12px;
	}

	.lbf-grid--top,
	.lbf-grid--sides {
		grid-template-columns: repeat(4, minmax(0, 1fr));
	}

	.lbf-grid--bottom {
		grid-template-columns: repeat(3, minmax(0, 1fr));
	}

	@media (max-width: 1024px) {
		.lbf-grid--top,
		.lbf-grid--sides {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}
	}

	@media (max-width: 640px) {
		.lbf-grid--top,
		.lbf-grid--sides,
		.lbf-grid--bottom {
			grid-template-columns: 1fr;
		}
	}

	.lbf-choice {
		display: block;
		cursor: pointer;
		margin: 0;
	}

	.lbf-choice input {
		position: absolute;
		opacity: 0;
		pointer-events: none;
	}

	.lbf-choice-body {
		display: flex;
		flex-direction: column;
		height: 100%;
		border: 2px solid #e5e7eb;
		background: #fff;
		transition:
			border-color 0.15s ease,
			box-shadow 0.15s ease;
	}

	.lbf-choice:hover .lbf-choice-body {
		border-color: #d1d5db;
	}

	.lbf-choice input:focus-visible + .lbf-choice-body {
		outline: 2px solid #fe5900;
		outline-offset: 2px;
	}

	.lbf-choice input:checked + .lbf-choice-body {
		border-color: #fe5900;
		box-shadow: 0 0 0 1px rgba(254, 89, 0, 0.25);
		background: linear-gradient(180deg, #fffaf7 0%, #ffffff 100%);
	}

	.lbf-choice-media {
		display: flex;
		align-items: center;
		justify-content: center;
		min-height: 120px;
		background: #f3f4f6;
		border-bottom: 1px solid #e5e7eb;
		padding: 10px;
	}

	.lbf-img {
		max-width: 100%;
		height: auto;
		max-height: 120px;
		object-fit: contain;
	}

	.lbf-img-missing {
		font-size: 12px;
		color: #6b7280;
		text-align: center;
		padding: 12px;
		line-height: 1.45;
	}

	.lbf-img-missing code {
		font-size: 11px;
		background: #e5e7eb;
		padding: 2px 5px;
	}

	.lbf-choice-text {
		padding: 12px 14px 14px;
		display: flex;
		flex-direction: column;
		gap: 4px;
	}

	.lbf-choice-title {
		font-family: 'Oswald', sans-serif;
		font-size: 14px;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.06em;
		color: #111827;
	}

	.lbf-choice-desc {
		font-size: 12px;
		color: #6b7280;
		line-height: 1.4;
	}

	.lbf-summary {
		padding: 18px 22px;
		margin-bottom: 16px;
		background: #fafafa;
		border: 1px solid #e5e7eb;
	}

	.lbf-summary-label {
		font-size: 11px;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.1em;
		color: #6b7280;
	}

	.lbf-summary-line {
		margin: 8px 0 0 0;
		font-size: 16px;
		color: #111827;
	}

	.lbf-footnote {
		font-size: 13px;
		color: #9ca3af;
		margin: 0;
	}
</style>
